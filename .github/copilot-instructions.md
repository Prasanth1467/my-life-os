

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v4.1.0 -->
<!-- Updated: 2026-04-17T19:31:58.929Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v4.1.0 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## app

### app\globals.css
```
var --background
var --foreground
var --card
var --card-foreground
var --popover
var --popover-foreground
var --primary
var --primary-foreground
var --secondary
var --secondary-foreground
var --muted
var --muted-foreground
var --accent
var --accent-foreground
var --destructive
var --border
var --input
var --ring
var --chart-1
var --chart-2
var --chart-3
var --chart-4
var --chart-5
var --radius
var --sidebar
```

### app\layout.tsx
```
component RootLayout
```

### app\page.tsx
```
component Page
hook useRouter
hook useEffect
```

### app\(app)\analytics\page.tsx
```
component AnalyticsPage
hook useLifeStore
hook useMemo
```

### app\(app)\angular\page.tsx
```
component AngularPage
hook useMemo
hook useLifeStore
```

### app\(app)\calendar\page.tsx
```
component CalendarPage
hook useLifeStore
hook useMonthNav
hook useMemo
hook useState
handler onClick
handler onPickDay
handler onOpenChange
```

### app\(app)\checkin\page.tsx
```
component CheckInPage
hook useRouter
hook useMemo
hook useLifeDerived
hook useLifeStore
hook useState
hook useEffect
handler onClick
handler onValueChange
handler onChange
```

### app\(app)\dashboard\page.tsx
```
component DashboardPage
hook useLifeDerived
hook useLifeStore
hook useMemo
hook useDailyQuote
```

### app\(app)\dsa\page.tsx
```
component DSAPage
hook useMemo
hook useLifeStore
```

### app\(app)\goals\page.tsx
```
component GoalsPage
component MissionCard
hook useMemo
hook useLifeStore
hook useState
handler onOpenChange
handler onChange
handler onClick
```

### app\(app)\intelligence\page.tsx
```
component IntelligencePage
hook useLifeDerived
hook useLifeStore
hook useMemo
```

### app\(app)\journal\page.tsx
```
component JournalPage
hook useMemo
hook useLifeStore
hook useState
handler onChange
handler onClick
```

### app\(app)\layout.tsx
```
component AppLayout
```

### app\(app)\mission\page.tsx
```
component MissionPage
component Stat
hook useLifeDerived
hook useLifeStore
hook useState
```

### app\(app)\smoke\page.tsx
```
component SmokePage
hook useMemo
hook useLifeStore
handler onChange
```

### app\(app)\today\page.tsx
```
component TodayPage
hook useMemo
hook useLifeDerived
hook useLifeStore
hook useState
hook useRef
hook useEffect
handler onOpenChange
```

### app\(app)\ui-kit\page.tsx
```
component UiKitPage
hook useState
handler onCheckedChange
handler onValueChange
```

### app\api\cron\maintenance\route.ts
```
export async function GET(request)
```

## components

### components\theme-provider.tsx
```
component ThemeProvider
component ThemeHotkey
hook useTheme
hook useEffect
```

### components\ui\button.tsx
```
component Button
handler onVariants
```

### components\calendar\heatmap30.tsx
```
component Heatmap30
hook useMemo
export Heatmap30
handler onIndex
```

### components\calendar\month-grid.tsx
```
component MonthGrid
hook useMonthNav
hook useState
hook useCallback
export MonthGrid
```

### components\charts\bars.tsx
```
component Bars
export Bars
```

### components\charts\sparkline.tsx
```
component Sparkline
export Sparkline
```

### components\live-clock.tsx
```
component LiveClock
hook useState
hook useEffect
export LiveClock
```

### components\quotes\daily-quote-hero.tsx
```
component DailyQuoteHero
export DailyQuoteHero
```

### components\shell\app-shell.tsx
```
component AppShell
hook useRouter
hook usePathname
hook useMemo
hook useState
hook useLifeStore
hook useEffect
export AppShell
handler onOpenChange
handler onClick
```

### components\shell\nav.ts
```
export type NavItem
```

### components\shell\sidebar.tsx
```
component Sidebar
component MobileNavButton
hook usePathname
export Sidebar
export MobileNavButton
handler onClick
```

### components\shell\topbar.tsx
```
component Topbar
hook useMemo
hook useLifeDerived
hook useLifeStore
export Topbar
handler onClick
```

### components\theme-toggle.tsx
```
component ThemeToggle
hook useTheme
export ThemeToggle
```

### components\ui\accordion.tsx
```
handler onItem
handler onTrigger
handler onContent
```

### components\ui\alert.tsx
```
component Alert
component AlertTitle
component AlertDescription
```

### components\ui\avatar.tsx
```
component Avatar
component AvatarImage
component AvatarFallback
```

### components\ui\badge.tsx
```
component Badge
```

### components\ui\card.tsx
```
component Card
component CardHeader
component CardTitle
component CardDescription
component CardContent
component CardFooter
```

### components\ui\dialog.tsx
```
component Dialog
component DialogTrigger
component DialogContent
component DialogHeader
component DialogTitle
component DialogDescription
component DialogFooter
component DialogClose
hook useState
hook useCallback
hook useContext
hook useEffect
handler onKey
handler onMouseDown
```

### components\ui\input.tsx
```
component Input
```

### components\ui\progress.tsx
```
component Progress
```

### components\ui\scroll-area.tsx
```
component ScrollArea
```

### components\ui\select.tsx
```
component Select
```

### components\ui\separator.tsx
```
component Separator
```

### components\ui\sheet.tsx
```
component Sheet
component SheetTrigger
component SheetContent
component SheetHeader
component SheetTitle
component SheetClose
hook useState
hook useCallback
hook useContext
hook useEffect
handler onKey
handler onMouseDown
```

### components\ui\skeleton.tsx
```
component Skeleton
```

### components\ui\sonner.tsx
```
component Toaster
export Toaster
```

### components\ui\switch.tsx
```
component Switch
```

### components\ui\tabs.tsx
```
component Tabs
component TabsList
component TabsTrigger
component TabsContent
hook useState
hook useCallback
hook useContext
```

### components\ui\textarea.tsx
```
component Textarea
```

### components\ui\tooltip.tsx
```
component TooltipProvider
component Tooltip
component TooltipTrigger
component TooltipContent
```

## lib

### lib\utils.ts
```
export function cn(...inputs)
```

### lib\data\angular.ts
```
export type AngularModule
```

### lib\data\dsa.ts
```
export type DSAModule
```

### lib\life\dates.ts
```
export function addDaysISO(day, deltaDays) → ISODate
export function diffDaysISO(a, b) → number
export function isTimeInWindow(now, windowStartHour, windowEndHour) → boolean
```

### lib\life\engine.ts
```
export function uid(prefix) → string
export function ensureDay(state, day) → LifeStateV1
export function computeLevel(xp)
export function applyXP(state, day, xpDelta, type, meta?) → LifeStateV1
export function updateStreakAfterCheckin(state, day, wasDone, perfectDay) → LifeStateV1
export function moodToScore(m) → number
```

### lib\life\migrations.ts
```
export type MigrationResult
export function migrateState(state) → MigrationResult
```

### lib\life\selectors.ts
```
export function xpForDay(state, day) → number
export function lastNDays(state, days) → Array<
```

### lib\life\stats.ts
```
export type DisciplineStats
```

### lib\life\types.ts
```
export type ISODate
export type DayStatus
export type TaskDomain
export type TodayTaskId
export type Mood
export type GoalCategory
export type GoalHorizon
export type MissionHorizon
export type LifeEventType
export type LifeEvent
export type JournalEntry
export type Goal
export type Mission
export type CheckIn
export type CheckInDraft
export type Daily
export type Gamification
export type Settings
export type LifeStateV1
export type LifeStateAny
```

### lib\life\validate.ts
```
export type ValidationResult
export function validateState(input) → ValidationResult<LifeStateAny>
```

### lib\quotes\ATTRIBUTION.md
```
h1 Quote sources (bundled offline)
```

### lib\quotes\quote-cycle.ts
```
export function getQuoteOfDay(opts) → DailyQuote
```

### lib\quotes\types.ts
```
export type QuoteCategory
export type DailyQuote
```

### lib\quotes\use-daily-quote.ts
```
export function useDailyQuote(dayNumber)
```

### lib\storage\idb.ts
```
export async function idbLoadState() → Promise<LifeStateAny | null>
export async function idbSaveState(state) → Promise<void>
export async function idbExport() → Promise<string>
export async function idbImport(jsonText) → Promise<void>
```

### lib\storage\storage.ts
```
export type PersistDriver
export async function detectDriver() → Promise<PersistDriver>
export async function loadState() → Promise<
export async function saveState(driver, state) → Promise<void>
export async function createBackup(driver) → Promise<string | null>
export async function exportData(driver) → Promise<string | null>
export async function importData(driver) → Promise<LifeStateAny | null>
```

### lib\storage\tauri.ts
```
export async function tauriLoadState() → Promise<LifeStateAny | null>
export async function tauriSaveState(state) → Promise<void>
export async function tauriCreateBackup() → Promise<string | null>
export async function tauriExportToFile() → Promise<string | null>
export async function tauriImportFromFile() → Promise<LifeStateAny | null>
```

### lib\store\lifeStore.tsx
```
component LifeHydrator
hook useLifeStore
hook useStore
hook useLifeDerived
hook useEffect
export CloudSyncStatus
export LifeHydrator
```

### lib\store\store.ts
```
export type Selector
export type Store
export function shallowEqual(a, b) → boolean
export function createStore(initial) → Store<T>
```

### lib\supabase\client.ts
```
export function createClient()
```

### lib\supabase\env.ts
```
export function getSupabaseUrl() → string | undefined
export function getSupabaseAnonKey() → string | undefined
export function isSupabaseBrowserConfigured() → boolean
```

### lib\supabase\middleware.ts
```
export async function updateSession(request)
```

### lib\supabase\server.ts
```
export async function createClient()
```

### lib\sync\cloud.ts
```
export type CloudAuthStatus
export async function ensureAnonymousSession(client) → Promise<CloudAuthStatus>
export async function fetchRemoteLifeState(client) → Promise<LifeStateV1 | null>
export async function pushLifeState(client, state) → Promise<boolean>
```

## scripts

### scripts\generate-quotes365.mjs
```
function norm(s)
function stripBOM(s)
function pickByKeywords(items, { include = [], exclude = [], maxLen = 240 } = {})
function uniqByText(items)
function takeUnique(items, n, seen)
function mapMitQuote(q, category)
function mapGitaQuote(q, verseMeta, category)
async function main()
```

## src-tauri

### src-tauri\Cargo.toml
```
table [package]
table [build-dependencies]
table [dependencies]
table [profile.release]
key name
key version
key description
key authors
key license
key repository
key edition
key tauri-plugin-dialog
key serde_json
key panic
key codegen-units
key lto
key opt-level
```

## supabase

### supabase\config.toml
```
table [api]
table [api.tls]
table [db]
table [db.pooler]
table [db.migrations]
table [db.seed]
table [db.network_restrictions]
table [realtime]
table [studio]
table [inbucket]
table [storage]
table [storage.s3_protocol]
table [storage.analytics]
table [storage.vector]
table [auth]
table [auth.rate_limit]
table [auth.email]
table [auth.sms]
table [auth.sms.twilio]
table [auth.mfa]
table [auth.mfa.totp]
table [auth.mfa.phone]
table [auth.external.apple]
table [auth.web3.solana]
table [auth.third_party.firebase]
```

### supabase\migrations\20260417191649_life_state_realtime.sql
```
TABLE public.life_state
TABLE public.cron_heartbeat
INDEX life_state_updated_at_idx ON public.life_state
FUNCTION public.life_state_set_updated_at()
TRIGGER life_state_set_updated_at
```
